#include <stdio.h>

void main() 

{
   int *ptr1, *ptr2, a,b,s;
   
  printf("\n enter 1st no:");
  scanf("%d",&a);
  printf("\n enter 2nd no:");
  scanf("%d",&b);
  
  ptr1=&a;
  ptr2=&b;
  
  s= *ptr1 + *ptr2;
  printf("\n sum is:%d",s);
  
}
