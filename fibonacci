#include <stdio.h>

int
main ()
{
  int a = 0, b = 1, i, n, nt;

  printf ("\n enter the range");
  scanf ("%d", &n);

  for (i = 1; i <= n; i++)
    {
      printf ("%d \n", a);
      nt = a + b;
      a = b;
      b = nt;

    }
  return 0;
}
